{% extends 'base.html.twig' %}

{% block title 'Home' %}

{% block body %}
    <h1 class="page-header">Home</h1>

    <ul>
        {% for product in products %}
            <li>{{ product.label }}</li>
        {% endfor %}
    </ul>
{% endblock %}
